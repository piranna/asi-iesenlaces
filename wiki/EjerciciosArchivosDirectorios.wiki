#summary Ejercicios con Archivos y Directorios.

= Ejercicios con Archivos y directorios =

  # Función que dado un directorio, muestre la lista de archivos del directorio ordenados por orden alfabético. Versión 2: por orden de creación.
  # Función que tiene como parámetro un directorio. Devuelve la lista de ficheros de ese directorio.
  # Función que dado un archivo devuelve el tiempo del último acceso.
  # Función que dado un archivo devuelve los segundos que han pasado desde el último acceso.
  # Función que dados dos archivos devuelve -1 si el primero es más reciente 0 si son iguales y 1 si el segundo es más reciente.